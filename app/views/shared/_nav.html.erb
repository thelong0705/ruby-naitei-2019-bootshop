<div id="logoArea" class="navbar">
  <a id="smallScreen" data-target="#topMenu" data-toggle="collapse" class="btn btn-navbar">
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
  </a>
  <div class="navbar-inner">
    <%= link_to image_tag("/images/logo.png"), root_path, class: "brand" %>
    <%= form_for :static_pages, url: root_path, method: :get, html: {class: "form-inline navbar-search"} do |f|  %>
      <%= search_field_tag :q, nil %>
      <%= f.submit "Go", class: "btn btn-primary", id: "submitButton" %>
    <% end %> 
    <ul id="topMenu" class="nav pull-right">
      <% if current_user %>
        <li class="">
          <%= link_to raw('<span class="btn btn-large btn-success">Sign out</span>'),
              destroy_user_session_path, method: :delete %>
        </li>
    <% else %>
        <li class="">
          <%= link_to "Sign up", new_user_registration_path %>
        </li>
        <li class="">
          <a href="#login" role="button" data-toggle="modal" style="padding-right:0">
            <span class="btn btn-large btn-success">Login</span>
          </a>
          <%= render "shared/login_modal" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
